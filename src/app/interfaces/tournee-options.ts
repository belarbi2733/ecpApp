/**
*Interface that carry every variables relatives to a round
*/
export interface TourneeOptions {
  idUser: number;
  idCar: number;
  depart: string;
  arrivee: string;
  date: string;
  id: number;
}

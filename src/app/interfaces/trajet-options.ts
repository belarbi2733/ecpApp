/**
*Interface that carry every variables relatives to a ride
*/
export interface TrajetOptions {
  idUser: number;
  idColis: number;
  idTour: number;
  depart: string;
  arrivee: string;
  date: string;
  id: number;
  code: number;
}
